{
  "name": "puppeteer-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx puppeteer browsers install chrome && node index.js"
  },
  "dependencies": {
    "puppeteer": "^24.8.2"
  }
}
